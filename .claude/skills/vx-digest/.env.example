# ===== 微信群消费配置 =====

# 群名列表（逗号分隔）
VX_GROUP_NAMES=""

# 飞书多维表格（从飞书表 URL 中获取 app_token 和 table_id）
VX_BITABLE_APP_TOKEN=
VX_BITABLE_TABLE_ID=
VX_BITABLE_STATE_TABLE_ID=

# 消费间隔（小时）
VX_CONSUME_INTERVAL=1

# vx-secret 密钥文件路径（由 vx-secret skill 的 extract_keys.py 生成）
VX_KEYS_FILE="~/.claude/skills/vx-secret/keys.json"
